#!/bin/bash

task_dir="$HOME/.task"

task stop "$@"
git -C $task_dir add .
git -C $task_dir commit -am "stopped task" >> "$HOME/.task/git.log"
git -C $HOME add .task
git -C $HOME commit -m "updating task module"
task limit:$LIM
