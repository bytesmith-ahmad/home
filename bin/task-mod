#!/bin/bash

task_dir="$HOME/.task"

task modify "$@"
git -C $task_dir add .
git -C $task_dir commit -m "modified task" >> "$HOME/.task/git.log"
git -C $task_dir push 2> "$HOME/.task/error.log"
git -C $HOME add .task
git -C $HOME commit -m "updating task module"
task limit:$LIM
